require 'spec_helper'

describe Rubypress do
  it 'has a version number' do
    expect(Rubypress::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
